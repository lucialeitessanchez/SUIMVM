<form method="post" action="{{ path('app_mpa_delete', {'id_mpa': mpa.id_mpa}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mpa.id_mpa) }}">
    <button class="btn">Delete</button>
</form>
